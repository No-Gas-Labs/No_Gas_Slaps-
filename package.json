{
  "name": "no-gas-slaps-telegram",
  "version": "1.0.0",
  "description": "No_Gas_Slaps™ - Telegram Play-to-Earn Multiplayer Game",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "build": "esbuild server/server.js --bundle --platform=node --format=esm --outfile=dist/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-telegram-bot-api": "^0.63.0",
    "socket.io": "^4.7.4",
    "telegraf": "^4.15.6"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
